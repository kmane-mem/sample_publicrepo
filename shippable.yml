language: python
#language: node_js

# Version number
#node_js:
  #- 8.1   
  #- 6.11 

python:
  - pypy3 
  
#Uncomment below section to disable the private submodules
git:
   submodules: false
   
build:
  ci:
    - python --version# Language setting
    - lsb_release -a
    - docker --version
    - docker info
    - docker ps
    #- node --version

    