language: python

# Version number
python:
  - 2.7

    
git:
   submodules: false
   
build:
  pre_ci_boot:
    image_name: arm64v8/ubuntu
    image_tag: 14.04
    pull: true
    
  ci:
    - python -V
    - docker ps
    - docker build -t=test .
    - docker images
